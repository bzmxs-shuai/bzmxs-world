/**
 * Placeholder for the future WordPress XML migration.
 *
 * This script intentionally does not scrape bzmxs.cn and does not fabricate an
 * import. The real importer should accept an exported WordPress XML file,
 * validate it, map categories to the three supported areas, and then upsert
 * posts into the CMS database idempotently.
 */

console.log("WordPress XML importer placeholder. Provide an XML export in a later migration phase.");
